import 'package:flutter/material.dart';

class TranslationsSettings extends StatelessWidget {
  const TranslationsSettings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
